package com.group26.heritage.module_e.service;

import org.springframework.stereotype.Service;

/**
 * Report Service — Module E
 * TODO: Inject ResourceRepository, CategoryRepository
 * TODO: getResourceStats() → Map<ResourceStatus, Long> (counts by status)
 * TODO: getDistributionReport(LocalDate from, LocalDate to) → report data
 * TODO: exportReportCsv(LocalDate from, LocalDate to) → byte[] CSV
 * TODO: exportReportExcel(LocalDate from, LocalDate to) → byte[] Excel
 */
@Service
public class ReportService {
    // TODO: implement reporting logic
}
