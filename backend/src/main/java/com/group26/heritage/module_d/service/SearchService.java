package com.group26.heritage.module_d.service;

import org.springframework.stereotype.Service;

/**
 * Search Service — Module D
 * TODO: Inject ResourceRepository, CategoryRepository, TagRepository
 * TODO: browseApproved(Pageable pageable) → Page<ResourceSummaryDto> (only APPROVED status)
 * TODO: searchByKeyword(String keyword, Pageable pageable) → Page<ResourceSummaryDto>
 * TODO: filterByCategoryAndTag(Long categoryId, Long tagId, Pageable pageable) → Page<ResourceSummaryDto>
 * TODO: getResourceDetail(Long resourceId) → Resource (only if APPROVED)
 */
@Service
public class SearchService {
    // TODO: implement search and discovery logic
}
