package com.group26.heritage.module_d.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * Comment Controller — Module D
 * Summary D-PBI 5 (Commenting and Feedback)
 *
 * TODO: GET /api/comments/resource/{resourceId} — get comments for a resource (D-PBI 5)
 * TODO: POST /api/comments/resource/{resourceId} — add comment (D-PBI 5)
 * TODO: POST /api/comments/{resourceId}/like — like a resource (D-PBI 5)
 * TODO: DELETE /api/comments/{resourceId}/unlike — unlike a resource (D-PBI 5)
 */
@RestController
@RequestMapping("/api/comments")
public class CommentController {
    // TODO: inject CommentService
    // TODO: implement endpoints
}
