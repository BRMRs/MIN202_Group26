package com.group26.heritage.module_a.service;

import org.springframework.stereotype.Service;

/**
 * User Service — Module A
 * TODO: Inject UserRepository, ContributorApplicationRepository
 * TODO: getProfile(Long userId) → User
 * TODO: updateProfile(Long userId, ProfileUpdateRequest) → User
 * TODO: applyForContributor(Long userId) → ContributorApplication
 * TODO: approveApplication(Long applicationId, Long adminId) — update role to CONTRIBUTOR
 * TODO: rejectApplication(Long applicationId, Long adminId) — update status to REJECTED
 * TODO: sendApprovalNotificationEmail(User user, boolean approved)
 */
@Service
public class UserService {
    // TODO: implement user management logic
}
