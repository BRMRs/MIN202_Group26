import axiosInstance from '../../common/api/axiosInstance';

/**
 * Resource API — Module B
 * Summary B-PBI 1 (Edit), B-PBI 2 (Draft), B-PBI 3 (Upload), B-PBI 4 (Submit), B-PBI 5 (Resubmit)
 */

void axiosInstance;

// TODO: export const createResource = (data) => axiosInstance.post('/resources', data);
// TODO: export const updateResource = (id, data) => axiosInstance.put(`/resources/${id}`, data);
// TODO: export const getMyResources = () => axiosInstance.get('/resources/my');
// TODO: export const saveDraft = (id, data) => axiosInstance.post(`/resources/${id}/draft`, data);
// TODO: export const submitForReview = (id) => axiosInstance.post(`/resources/${id}/submit`);
// TODO: export const resubmitResource = (id, data) => axiosInstance.put(`/resources/${id}/resubmit`, data);
// TODO: export const uploadFile = (id, formData) => axiosInstance.post(`/resources/${id}/upload`, formData, { headers: { 'Content-Type': 'multipart/form-data' } });
// TODO: export const deleteResource = (id) => axiosInstance.delete(`/resources/${id}`);
