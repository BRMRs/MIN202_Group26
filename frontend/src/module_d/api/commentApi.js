import axiosInstance from '../../common/api/axiosInstance';

/**
 * Comment API — Module D
 * Summary D-PBI 5: Basic Commenting and Feedback
 */

void axiosInstance;

// TODO: export const getComments = (resourceId) => axiosInstance.get(`/comments/resource/${resourceId}`);
// TODO: export const addComment = (resourceId, content) => axiosInstance.post(`/comments/resource/${resourceId}`, { content });
// TODO: export const likeResource = (resourceId) => axiosInstance.post(`/comments/${resourceId}/like`);
// TODO: export const unlikeResource = (resourceId) => axiosInstance.delete(`/comments/${resourceId}/unlike`);
