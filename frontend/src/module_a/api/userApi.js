import axiosInstance from '../../common/api/axiosInstance';

/**
 * User API — Module A
 * Summary A-PBI 1.4 (Profile), A-PBI 1.5 (Contributor Application)
 */

void axiosInstance;

// TODO: export const getProfile = () => axiosInstance.get('/users/me');
// TODO: export const updateProfile = (data) => axiosInstance.put('/users/profile', data);
// TODO: export const applyForContributor = () => axiosInstance.post('/users/apply-contributor');
// TODO: export const getApplications = () => axiosInstance.get('/admin/users/applications');
// TODO: export const approveApplication = (id) => axiosInstance.put(`/admin/users/applications/${id}/approve`);
// TODO: export const rejectApplication = (id) => axiosInstance.put(`/admin/users/applications/${id}/reject`);
