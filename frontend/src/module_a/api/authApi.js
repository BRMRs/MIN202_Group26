import axiosInstance from '../../common/api/axiosInstance';

/**
 * Auth API — Module A
 * Summary A-PBI 1.1 (Register), A-PBI 1.2 (Login), A-PBI 1.3 (Logout)
 */

void axiosInstance;

// TODO: export const register = (data) => axiosInstance.post('/auth/register', data);
// TODO: export const login = (credentials) => axiosInstance.post('/auth/login', credentials);
// TODO: export const logout = () => axiosInstance.post('/auth/logout');
// TODO: export const verifyEmail = (token) => axiosInstance.get(`/auth/verify-email?token=${token}`);
