import axiosInstance from '../../common/api/axiosInstance';

/**
 * Category API — Module E
 * Summary E-PBI 1: Category Management
 */

void axiosInstance;

// TODO: export const getCategories = () => axiosInstance.get('/admin/categories');
// TODO: export const createCategory = (data) => axiosInstance.post('/admin/categories', data);
// TODO: export const updateCategory = (id, data) => axiosInstance.put(`/admin/categories/${id}`, data);
// TODO: export const deleteCategory = (id) => axiosInstance.delete(`/admin/categories/${id}`);
