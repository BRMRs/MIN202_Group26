import axiosInstance from '../../common/api/axiosInstance';

/**
 * Dashboard API — Module E
 * Summary E-PBI 4 (Dashboard), E-PBI 5 (Reporting)
 */

void axiosInstance;

// TODO: export const getStats = () => axiosInstance.get('/admin/dashboard/stats');
// TODO: export const getReport = (from, to) => axiosInstance.get('/admin/dashboard/reports', { params: { from, to } });
// TODO: export const exportReport = (from, to) => axiosInstance.get('/admin/dashboard/reports/export', { params: { from, to }, responseType: 'blob' });
