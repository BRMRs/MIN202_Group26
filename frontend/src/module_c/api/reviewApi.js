import axiosInstance from '../../common/api/axiosInstance';

/**
 * Review API — Module C
 * Summary C-PBI 1 (Dashboard), C-PBI 2 (Approve/Reject), C-PBI 3 (Feedback), C-PBI 5 (Archive)
 */

void axiosInstance;

// TODO: export const getPendingResources = (params) => axiosInstance.get('/reviews/pending', { params });
// TODO: export const getResourceForReview = (resourceId) => axiosInstance.get(`/reviews/${resourceId}`);
// TODO: export const approveResource = (resourceId, feedback) => axiosInstance.post(`/reviews/${resourceId}/approve`, feedback);
// TODO: export const rejectResource = (resourceId, feedback) => axiosInstance.post(`/reviews/${resourceId}/reject`, feedback);
// TODO: export const getReviewFeedback = (resourceId) => axiosInstance.get(`/reviews/${resourceId}/feedback`);
// TODO: export const archiveResource = (resourceId, reason) => axiosInstance.post(`/reviews/${resourceId}/archive`, { reason });
